"""swarm-lab.

A scaling observatory: what actually happens to a multi-agent system as N grows.

The deterministic core lives in :mod:`swarmlab.domain`. Everything the model is
allowed to touch sits above it.
"""

__all__ = ["domain"]
